# nola_filter.py

def apply_filter(text):
    """
    Простейший фильтр для архитектурной дисциплины.
    Пока — только очищает пробелы и приводит к базовой форме.
    В будущем будет учитывать контекст, этап мышления и внутренние принципы.
    """
    filtered = text.strip()

    # Пока не блокируем ничего — только подчёркиваем, что прошёл фильтр
    return filtered